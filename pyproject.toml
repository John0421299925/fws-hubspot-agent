[project]
name = "fws-hubspot-agent"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
    "requests>=2.31.0",
    "anthropic>=0.40.0",
]

[tool.vercel]
entrypoint = "api.webhook:app"
